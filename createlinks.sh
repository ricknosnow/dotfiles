#!/bin/sh

#This script can be used to create the
#symbolic links. Comment out what you don't
#want.
#Run this as the user you want to create
#the links for.
ln -s ~/.dotfiles/.tmux.conf ~/.tmux.conf  
ln -s ~/.dotfiles/.vimrc ~/.vimrc 
